<%= form_for @url, remote: true, html: { id: 'urls-form' } do |f| %>
  <div class='flash'>
    
  </div>

  <div class="field">
    <%= f.label :target %><br>
    <%= f.text_area :target %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
